<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class attendence extends Model
{
    //
  /* function get_usersbyname($request){
        dd($request);
        $resultset = DB::table('users')->where(function($query) use ($name) {
            $query->where('username', 'LIKE', $name);
        });*/
}
